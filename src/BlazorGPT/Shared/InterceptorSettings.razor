<button type="button" class="btn btn-secondary @Class" @onclick="OpenSettings">Interceptors</button>
@code
{
    [Parameter]
    public string? Class { get; set; }

    [Inject]
    DialogService? DialogService { get; set; }

    private async Task OpenSettings()
    {
        await DialogService!.OpenAsync<InterceptorSelector>("Select inteceptors", options:new DialogOptions()
        {
            CloseDialogOnOverlayClick = true,
        });
    }
}
